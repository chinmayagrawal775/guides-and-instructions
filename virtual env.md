package used to create virtual envs = ```pip installvirtualenv```

Make new virtual env
    - go to path where you want to create the envs
    - run the below command
    - virtualenv <env name>

Activate the env
    - <env name>\Scripts\activate